{
  // Emits dist. Tests, their harness, and dev-only scripts live inside src, so
  // they are excluded here instead of shipping them; tsconfig.json still type
  // checks them.
  "extends": "./tsconfig.json",
  "exclude": ["src/**/*.test.ts", "src/testing/**", "src/scripts/**"]
}
