{
  // Emits dist. Tests and their harness live inside src, so they are excluded
  // here instead of shipping them; tsconfig.json still type checks them.
  "extends": "./tsconfig.json",
  "exclude": ["src/**/*.test.ts", "src/testing/**"]
}
